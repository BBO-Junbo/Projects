spring:
  datasource:
    url: jdbc:mysql://localhost:3306/book_db?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&requireSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations:
    - classpath:/Mapper/*.xml

logging:
  level:
    cn.tedu.pringboot01.mapper: DEBUG

